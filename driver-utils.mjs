/**
 * Helper to define a driver with future validation hooks.
 * @param {import('./types.d.ts').Driver} driver
 * @returns {import('./types.d.ts').Driver}
 */
export function defineDriver(driver) {
  return driver;
}
